# Normalize line endings. Text files are stored with LF in the repo; shell
# scripts and SQL must stay LF so they run correctly in Linux containers.
* text=auto eol=lf
*.sh text eol=lf
*.sql text eol=lf
